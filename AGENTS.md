# Agent 协作规范

## 主线程

- 主线程对最终结果负责，可按任务耦合度、上下文成本与并行收益灵活选择直接完成或委派。
- 小型改动、紧密耦合工作、关键整合与冲突处理可由主线程直接实施；边界清晰且委派有质量或效率收益时使用子代理。
- 审核以代码、diff、验证结果和子任务报告为主要依据，并按完成任务所需加载上下文。

## 委派与模型

- 除前端视觉实现外，凡委派调研、审阅或开发实现，统一使用 Codex 原生子代理，默认继承主代理当前的 Codex 模型和推理级别。
- 前端页面、组件的视觉与排版实现统一交给 WorkBuddy 的 `hy3`；不使用其他 WorkBuddy/CodeBuddy 模型，也不把 `hy3` 作为后端、审批或完成条件。
- 不使用 `codex-pi-ds`、Pi CLI、DeepSeek、ChatGPT Pro 或其他外部模型与工具链充当后端开发子代理、审批者或完成条件。
- 只有任务风险、难度或延迟收益明确需要时，才调整可用的 Codex 模型或推理强度；不固定模型分工，也不因外部模型不可用而阻塞开发。

## 前后端分工

- Codex 负责描述并实现后端功能点，包括数据模型、API 契约、账户与 session 隔离、权限、安全、持久化、执行链路和后端验收。
- 需要前端实现时，Codex 只向 `hy3` 提供必要的用户目标、交互流程、数据契约、状态/错误边界、无障碍要求和功能验收；不预设具体布局、配色、间距、组件组合或视觉风格，让 `hy3` 自主完成排版与视觉设计。
- Codex 可审核前端是否满足功能契约，并使用 Playwright CLI 验证真实交互、DOM/ARIA、URL、控制台和网络行为；除非用户另行明确要求，不直接实施或反复微调前端视觉样式。
- `hy3` 的前端改动仍须保护现有 WIP，并通过项目约定的最小充分验证；视觉偏好不覆盖后端契约、安全边界和核心交付要求。

## 子任务要求

- 每个子任务必须边界清晰；并行任务不得修改同一文件。
- 互不冲突的任务可在独立临时 Git worktree/临时分支并行开发；主线程审核合并后，确认改动已合并再删除对应 worktree 和分支。
- 相似或连续任务在有明确上下文、session 或缓存收益时优先复用同一 Codex 子代理会话；不冲突任务有并行收益时可新建会话，不得为复用而串行化独立工作。
- 子代理回报优先包含结论、改动文件、验证、风险或阻塞，并按需附上必要诊断；避免粘贴无关的冗长日志。
- 主线程负责处理跨任务冲突并决定合并结果。

## 项目执行规则

- 开发新功能时，优先调研 GitHub 上的开源方案，避免重复造轮子。
- 不影响核心结果的低风险细节（命名、格式、文档同步、中英文一致性）自主决定并直接执行。
- 对已有明确执行指令，不反复确认；仅在重大取舍、破坏性操作、外部权限或发布风险时询问。
- 简单、低风险的改动不额外编写设计文档。
- 默认按“大模块/完整纵向切片”推进，并在模块完成后设计一次最小充分验证矩阵，集中覆盖必要的单元、集成与端到端要点；测试设计与执行不需要预先批准。
- 集中验证是减少重复成本的默认节奏，不是阻止反馈的硬门槛；开发中可随时运行低成本、高信号的静态检查或定向验证，高风险修复及失败后的修复可按需回归。
- 浏览器验收默认使用 Playwright CLI 驱动本机 Chrome 无头运行与精简 reporter；优先通过 DOM、ARIA locator、URL、控制台和网络断言判断结果，不以反复截图或图片识别作为主验证方式。成功时不保留截图或 trace，失败时只保留必要诊断产物。

## 安全

- 保护已有 WIP，不覆盖或清理用户及其他代理的未合并改动；除非用户明确授权，不使用 reset、checkout 或 clean 等破坏性 Git 操作。
- 密钥不得写入代码、日志或文档，不提交 `.env.local`。

## 产品运行时命名

- `fomo-pi-ds` 仅指 FOMO 产品在生成沙箱中调用的 Pi + DeepSeek runtime。该名称不代表开发代理、审批流程、状态 sidecar 或 Codex 子代理会话；产品 runtime 的选择不得反向限制 Codex 开发流程。
