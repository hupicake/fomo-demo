# Agent 协作规范

## 主线程

- 只负责理解目标、任务拆解与委派、审核、冲突处理、合并、最终验收和用户沟通。
- 不承担可独立委派的大段调研或实现，保持主线程上下文干净。
- 仅加载审核所需的最小上下文，以 diff、测试和子任务报告验收结果。

## 委派与模型

- 调研默认委派给 `gpt-5.6-terra`，`reasoning_effort=max`。
- 开发实现统一使用 Pi CLI 的 `deepseek/deepseek-v4-flash`，`thinking=max`，不再使用 WorkBuddy/CodeBuddy CLI 的 DeepSeek。
- Pi 或 DeepSeek Flash 不可用时，直接回退到 Terra Max，不得因此阻塞。
- 严禁使用任何 Pro 模型。

## Pi 命名

- `codex-pi-ds`：Codex 开发 FOMO 时使用的本机 Pi CLI + DeepSeek，属于开发工具链。
- `fomo-pi-ds`：FOMO 产品运行时在生成沙箱中使用的 Pi + DeepSeek，属于产品运行时。
- 两者的会话、权限、状态和凭据完全隔离；后续文档、任务和状态输出必须使用上述名称，不要只写含糊的“Pi”。

## 子任务要求

- 每个子任务必须边界清晰；并行任务不得修改同一文件。
- 互不冲突的任务可在独立临时 Git worktree/临时分支并行开发；主线程审核合并后，确认改动已合并再删除对应 worktree 和分支。
- 相似或连续任务优先复用同一子代理会话，以复用上下文和缓存、减少 token 消耗。
- 子代理回报只包含：结论、改动文件、验证、风险或阻塞；不要粘贴冗长日志。
- 主线程负责处理跨任务冲突并决定合并结果。

## 项目执行规则

- 开发新功能时，优先调研 GitHub 上的开源方案，避免重复造轮子。
- 不影响核心结果的低风险细节（命名、格式、文档同步、中英文一致性）自主决定并直接执行。
- 对已有明确执行指令，不反复确认；仅在重大取舍、破坏性操作、外部权限或发布风险时询问。
- 简单、低风险的改动不额外编写设计文档。
- 避免为单个小功能反复测试；测试设计与执行须先获得 Grok 明确同意，再按批准范围集中验证。
- 浏览器验收使用 Playwright CLI 无头运行与精简 reporter；成功不保留截图或 trace，失败只保留必要诊断产物。

## 安全

- 密钥不得写入代码、日志或文档，不提交 `.env.local`。
